<header class="d-flex justify-content-between align-items-center">
<div class="logo">
 <a href="<%- url_for() %>"><%= config.title %></a>
</div>

<nav class="d-inline-flex">
  <ul class="nav">
    <% for (item in theme.menu) { %>
    <li class="nav-item">
      <a class="nav-link" href="<%- url_for(theme.menu[item]) %>"> <%= item %></a>
    </li>
    <% } %>
  </ul>
</nav>
</header>

